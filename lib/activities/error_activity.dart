import 'package:flutter/material.dart';

class ErrorActivity extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Error activity'),
    );
  }
}
