import 'package:flutter/material.dart';

class WorkActivity_Android extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Мы вошли в приложение'),
    );
  }
}
