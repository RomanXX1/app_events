import 'package:flutter/material.dart';

class LinkActivity_Android extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Link_Android'),
    );
  }
}
