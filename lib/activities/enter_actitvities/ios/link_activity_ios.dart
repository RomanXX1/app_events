import 'package:flutter/cupertino.dart';

class LinkActivity_iOS extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Link_iOS'),
    );
  }
}
