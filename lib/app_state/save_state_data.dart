class SetLogin {
  final String login;
  SetLogin(this.login);
}

class SetPass {
  final String pass;
  SetPass(this.pass);
}

class SetNameDB {
  final String nameDB;
  SetNameDB(this.nameDB);
}

class SetPathDB {
  final String pathDB;
  SetPathDB(this.pathDB);
}

class SetToken {
  final String token;
  SetToken(this.token);
}
